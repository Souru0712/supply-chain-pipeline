# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Secrets / local config
.env
.env.*
!.env.example
.claude

# dbt
target/
dbt_packages/
logs/
*.duckdb
*.duckdb.wal

# Data (stored in S3, not git) — forecast outputs are committed for dashboard
data/materialized/
data/staged/
data/raw/
starter/

# MLflow run store (regenerable)
mlruns/

# Regenerable model files — exclude large binaries but keep XGBoost JSON models
# models/ is intentionally tracked to ship trained artifacts with the repo

# Great Expectations local artifacts
gx/uncommitted/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Personal notes
project_plan.txt
dbt_integration_plan.md