name: supply_chain
version: '1.0.0'
profile: supply_chain

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

vars:
  ingredient_name: "CORN"
  pipeline_root: ".."

models:
  supply_chain:
    staging:
      +materialized: view
    marts:
      +materialized: external